%% -*- mode: erlang -*-
%% rebar3 configuration for mcl_victron.
%%
%% Victron GX telemetry (dbus-flashmq MQTT on the LAN), recorded in its own
%% reckon-db store and published on the mesh.

{erl_opts, [debug_info, warnings_as_errors]}.

{deps, [
    %% The shared L2 substrate: mesh SDK, realm identity, health, and the
    %% reckon-db store mcl_om:boot/1 opens for a service exporting store_id/0.
    {mcl_om, "~> 0.26.6"},
    %% 1.24: a restart's replayed events are marked, and a handler can skip
    %% them (replay_policy/0). The emitter does: history is not live telemetry.
    {evoq, "~> 1.24"},
    {reckon_evoq, "~> 2.7"}
]}.

{project_plugins, [rebar3_lint]}.

%% The org's structural ruleset, matching the sibling services.
{elvis, [
    {rulesets, #{
        mcl_min => [
            {elvis_style, no_deep_nesting, #{level => 2}},
            {elvis_style, no_nested_try_catch, #{}},
            {elvis_style, no_if_expression, #{}}
        ]
    }},
    {config, [
        #{files => ["apps/*/src/**/*.erl"], ruleset => mcl_min}
    ]}
]}.

{eunit_opts, [verbose]}.

{relx, [
    {release, {mcl_victron, "0.1.0"}, [mcl_victron, sasl]},
    {sys_config_src, "config/sys.config.src"},
    {vm_args_src,    "config/vm.args.src"},
    {extended_start_script, true}
]}.

{profiles, [
    {test, [
        %% meck stands in for the mesh in the emitter tests.
        {deps, [{meck, "~> 1.2"}]},
        {eunit_opts, [{sys_config, "./config/test.sys.config"}]}
    ]},
    {prod, [{relx, [{mode, prod},
                    {include_src, false},
                    {include_erts, true},
                    {debug_info, strip}]}]}
]}.

{dialyzer, [
    {warnings, [no_unknown]},
    {plt_extra_apps, [mcl_om, macula, evoq, reckon_evoq, reckon_db]}
]}.
